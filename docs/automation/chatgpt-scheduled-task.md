# Scheduled task prompt

Use the following as the complete scheduled-task instruction. The repository contract is intentionally authoritative so reliability changes do not require another oversized task-panel edit.

```text
Operate the Portfolio Dashboard without user supervision.

At the beginning of every invocation, use the connected GitHub app to read the latest `main` revision of `samrosenstock10/sam-portfolio-dashboard`, then read `AUTOMATION.md`. Treat that file as the authoritative operating contract and follow it exactly. The latest repository contract overrides this prompt.

Use connected Finances, Google Sheets, GitHub, Vercel, Gmail, and public sources only as the contract permits. Preserve all existing return methodology, historical calibration, cash-flow classifications, Friday source rules, privacy constraints, Sheet structure, dashboard design, and stable project identities. Never create a replacement spreadsheet, repository, Vercel project, automation, or guessed data.

Execute only the incomplete layer of the current logical cycle. Perform the contract's cheap state classification before expensive work. Stop silently when complete or when a non-final recovery wake is merely waiting for provider/source freshness. Validate every changed artifact through the repository workflow and publish at most one changed `index.html` for one verified data version. Notify only under the contract's final-failure rules.
```
